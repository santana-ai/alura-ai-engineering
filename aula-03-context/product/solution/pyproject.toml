[project]
name = "clinica-alura"
version = "0.3.0"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.0,<2.0",
    "langchain-openai>=1.0,<2.0",
    "click>=8.1",
    "langgraph>=1.0,<2.0",
    "langgraph-checkpoint-sqlite>=2.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
    "pytest>=8.0",
]

[project.scripts]
clinica-alura = "clinica_alura.app:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/clinica_alura"]
